pipeline {
	agent any
	    stages {
		   stage('Build Docker image') {
			steps {
			script {
   				docker.build('fibonacci:3.0', '.')
			}
			}
		   }
		   stage('Push to Docker Hub') {
			steps {
				script{
				docker.withRegistry('https://index.docker.io/v1/', 'Docker_Login') {
                    		}
				}
				sh 'docker push shreyam2001/fibonacci-2:3.0'
			}
		   }
	     }
}
