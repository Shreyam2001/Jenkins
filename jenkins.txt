pipeline {
	agent any
	    stages {
		   stage('Build Docker image') {
			steps {
			script {
   				def dockerImage = docker.build('fibonnaci:2.1', '.')
			}
			}
		   }
		   stage('Push to Docker Hub') {
			steps {
				docker.withRegistry('shreyam2001/fibonacci-2', 'Docker_Login') {
                        		docker.push('fibonnaci:2.1')
                    		}
			}
		   }
	     }
}
